name: Copilot PR Review and Record

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: write
  pull-requests: write

jobs:
  record-review:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Get PR information
        id: pr-info
        run: |
          echo "pr_number=${{ github.event.pull_request.number }}" >> $GITHUB_OUTPUT
          echo "date=$(date +%Y-%m-%d)" >> $GITHUB_OUTPUT
          echo "datetime=$(date '+%Y-%m-%d %H:%M')" >> $GITHUB_OUTPUT

      - name: Get changed files
        id: files
        uses: actions/github-script@v7
        with:
          script: |
            const { data: files } = await github.rest.pulls.listFiles({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: context.issue.number
            });
            return files.map(f => f.filename);

      - name: Update REVIEW_HISTORY.md
        env:
          PR_NUMBER: ${{ steps.pr-info.outputs.pr_number }}
          DATE: ${{ steps.pr-info.outputs.date }}
          DATETIME: ${{ steps.pr-info.outputs.datetime }}
        run: |
          FILES='${{ steps.files.outputs.result }}'
          
          echo "" >> .github/REVIEW_HISTORY.md
          echo "### PR #${PR_NUMBER} - ${DATETIME}" >> .github/REVIEW_HISTORY.md
          
          echo "$FILES" | jq -r '.[]' | while read -r file; do
            echo "- \`${DATE} | ${file} | Copilot 자동 리뷰 진행 → 🔄 검토중\`" >> .github/REVIEW_HISTORY.md
          done
          
          echo "" >> .github/REVIEW_HISTORY.md
          echo "---" >> .github/REVIEW_HISTORY.md

      - name: Commit changes
        run: |
          git config --local user.name 'github-actions[bot]'
          git config --local user.email 'github-actions[bot]@users.noreply.github.com'
          
          if git diff --quiet .github/REVIEW_HISTORY.md; then
            echo "No changes to commit"
          else
            git add .github/REVIEW_HISTORY.md
            git commit -m "📝 Update REVIEW_HISTORY.md for PR #${{ steps.pr-info.outputs.pr_number }}"
            git push
          fi

      - name: Add PR comment
        uses: actions/github-script@v7
        env:
          PR_NUM: ${{ steps.pr-info.outputs.pr_number }}
        with:
          script: |
            const prNumber = process.env.PR_NUM;
            const commentBody = [
              '🤖 **Copilot 자동 리뷰 기록**',
              '',
              '이 PR의 리뷰 정보가 `.github/REVIEW_HISTORY.md`에 기록되었습니다.',
              '',
              '**리뷰를 시작하려면:**',
              '- 이 PR에 `@github-copilot`를 멘션하여 질문하세요',
              '- GitHub Copilot 기능을 활성화하세요',
              '',
              '**참고사항:**',
              '- 코드 품질, 보안, 성능 개선 사항을 확인하세요',
              '- 리뷰 후 REVIEW_HISTORY.md의 상태를 업데이트하세요',
              '  - ✅ 수정완료',
              '  - 🔄 검토중',
              '  - ❌ 미수정',
              '  - ⚠️ 보안 이슈',
              '  - 🚀 성능 개선'
            ].join('\n');
            
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: parseInt(prNumber),
              body: commentBody
            });
